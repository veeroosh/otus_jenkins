---
- scm:
    name: uploader_scm
    scm:
      - git:
          url: https://github.com/veeroosh/otus_jenkins
          credentials-id: jenkins
          branches:
            - FETCH_HEAD
          refspec: ${REFSPEC}