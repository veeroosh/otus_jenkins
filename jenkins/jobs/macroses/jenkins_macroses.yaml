---
- scm:
    name: uploader_scm
    scm:
      - git:
          url: https://github.com/veeroosh/repository
          credentials-id: jenkins
          branches:
            - FETCH_HEAD
          refspec: ${REFSPEC}