- job:
    name: jobs_uploader
    project-type: pipeline
    description: "Jobs uploader"
    concurrent: false
    parameters:
      - text:
          name: REFSPEC
          default: master
          description: "Branch"
    pipeline-scm:
      scm:
        - uploader_scm
      script-path: pipelines/jobs_uploader.groovy